#Roger Van Peski, 2/21/14
R.<x1,x2,x3> = ZZ[q,t]


##def change_variables(poly,maxcoef,maxpower):
##    '''takes a string which is a polynomial in mathematica,
##    and outputs one which works in sage.'''
##    for coef in range(maxcoef):
##        for power in range(maxpower):
            
